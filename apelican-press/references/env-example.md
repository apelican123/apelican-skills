# 环境变量模板

把下面两行保存成本机技能目录里的 `.env`。等号后面换成你自己的值。不要把填好的文件传回聊天或上传到技能商店。

```text
WECHAT_APP_ID=your_wechat_app_id
WECHAT_APP_SECRET=your_wechat_app_secret
```

Windows 记事本「另存为」时，文件类型选「所有文件」，文件名只写 `.env`。
